version: "3.1"

services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: cover
      POSTGRES_USER: cover
      POSTGRES_DB: cover
